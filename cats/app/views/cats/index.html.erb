<a href="<%= new_cat_url %>">Add New Cat</a>

<h1>Here are all our cats!</h1>

<ul>
  <% @cats.each do |cat|%>
    <li>
      <%= cat.name %>
    </li>
    <li>
    <a href="<%= cat_url(cat) %>">Cat Details</a>
    </li>
  <% end %>
</ul>